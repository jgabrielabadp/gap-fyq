package com.gap.fyq.model.thirdeso.matter;

public enum MatterType {
    MASS_PERCENTAGE,     // % en masa = m_soluto / m_disolución × 100
    VOLUME_CONCENT_GL,   // C (g/L) = m_soluto / V (L)
    GAS_LAWS_ADVANCED    // Boyle, Charles, Gay-Lussac — cualquier variable incógnita
}

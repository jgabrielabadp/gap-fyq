package com.gap.fyq.model.fourtheso.scientificactivity;

public enum FourthEsoActivityType {

    /** Opción múltiple: identificar y justificar si una magnitud es escalar o vectorial. */
    SCALAR_VECTORIAL,

    /** Cálculo del módulo del vector resultante: misma dirección (mismo o distinto sentido)
     *  o vectores perpendiculares (Teorema de Pitágoras A²+B²=R²). */
    VECTOR_MATH,

    /** Contar las cifras significativas de un número dado en forma decimal o notación científica. */
    SIGNIFICANT_FIGURES
}
